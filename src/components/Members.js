import React from "react";

const Members = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "90vh",
      }}
    >
      <h1>Members Only</h1>
    </div>
  );
};

export default Members;